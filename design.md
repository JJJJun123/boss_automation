# Boss 直聘智能岗位匹配 — 需求文档

## 背景

Boss 直聘的算法推荐受付费排名污染，大量推送无关岗位；求职者在没有外部工具支持下需要每天手动刷 1-2 小时筛选可投递岗位。

## 问题与价值

**问题：** 求职者在 Boss 直聘搜索关键词后，平台仅展示岗位列表（按算法/付费排名混合排序）。用户需要逐一打开岗位、阅读 JD，才能判断是否值得投递。

**价值：** 通过 AI 自动阅读用户简历 + 每个岗位的 JD，给出 1-10 分匹配评分和具体说明，帮助用户在大量搜索结果中快速决定投递优先级。

## 差异化价值

- Boss 直聘平台没有面向求职者的智能匹配机制，算法服务于雇主排序
- 本工具天然站求职者一边，对每个岗位给出 1-10 分评分 + 具体匹配亮点和差距说明
- 跨关键词搜索结果统一排序，帮助决定投递优先级

## 目标用户

正在主动求职、需要在大量岗位中高效决策的用户。行为模式：高分岗位重点投递，低分岗位也会投递（平衡型）。

---

## 部署形态

**云端多用户 Web 应用**：`https://boss.jjjj789.win`

- 单个 VPS（Vultr Tokyo 4GB / 2 vCPU）+ Cloudflare CDN
- 每个用户独立 Chrome profile，服务端按 `user_id` → UUID 映射隔离登录态
- 并发上限：全局 2-3 个 Chrome 进程同时跑（4GB RAM 限制）；同用户同时只允许 1 个任务
- 不公开传播，定向邀请使用（邀请码或一次性链接 token）

---

## 核心功能需求

### F1：简历上传

- 支持 PDF / DOCX 格式，单文件 ≤ 5MB
- 解析简历全文（直接传文本给 AI，不做额外结构化）
- 服务端按 `user_id` 隔离存储解析后的文本，TTL 24 小时；不保存原文件
- 提供"删除简历"接口，立即清

### F2：用户求职意向

每位用户首次使用时填写：

**必填**

```
求职目标方向（单选）：
[ ] 算法/AI（大模型、推荐、计算机视觉）
[ ] 产品经理（C 端、B 端、AI 应用）
[ ] 数据分析（业务、商业智能）
[ ] 工程开发（后端、前端、运维）
[ ] 其他：______（自填）
```

**Hard filters（决定岗位是否进入评分队列，不达标直接排除）**

- 最低薪资门槛（K，按月）：默认空
- 期望年限范围（如 1-3 年）
- 排除标签（多选）：[外包] [培训] [中介/猎头自营] [销售岗]
- 学历下限（可选）

**Soft preferences（影响评分但不排除）**

- 期望城市
- 公司规模偏好（创业 / 中型 / 大厂）
- 远程/混合工作意愿

Hard filters 在 AI 评分前过滤，节省 API 成本。Soft preferences 进 AI prompt 作权重。

### F3：Boss 登录态获取（QR 透传）

每个用户首次使用要扫码登录自己的 Boss 账号。

**Profile 隔离**

- 每个 `user_id` 对应一个服务端生成的 UUID profile 目录：`browser_profile/<uuid>/`
- UUID 不暴露给前端，user_id → uuid 映射存在服务端
- 单 profile 同时只允许 1 个 Chrome 进程持有（per-profile 互斥锁，避免锁冲突）
- 同用户第二个任务进队列，不并发抢同一 profile

**登录流程状态机**

| 状态             | 触发                          | 前端行为                      | 后端动作                            |
| ---------------- | ----------------------------- | ----------------------------- | ----------------------------------- |
| `qr_ready`       | Chrome 加载完登录页 + 截到 QR | 显示 QR + 倒计时              | 启动 90s 过期定时器                 |
| `qr_expired`     | 90s 内未扫                    | 显示"过期，重试"              | 重新加载登录页生成新 QR             |
| `login_pending`  | 扫码完成、等 Boss 确认        | 显示"等待 Boss 确认..."       | 监听 cookie 变化                    |
| `security_check` | Boss 触发风控（短信/验证码）  | 显示提示、等用户在 App 内通过 | 继续轮询                            |
| `login_success`  | cookie 已落 profile           | 自动转到搜索界面              | 写 `last_login_at`                  |
| `login_failed`   | 上述 90 秒内任何失败          | 显示原因 + 重试按钮           | 清理临时状态                        |
| `user_cancelled` | 用户点取消                    | 关闭 QR 弹窗                  | 关 Chrome、保留 profile（已扫的话） |

**提供"删除我的 Boss 登录态"按钮**：清空对应 UUID profile 目录。

### F4：岗位搜索

- 用户输入：关键词、城市、数量（默认 30，上限 50）
- 系统通过 Playwright（patchright）在该用户 Chrome profile 上自动爬取 Boss 直聘
- 搜索过程通过 WebSocket 实时推送进度（仅状态：抓取中 / 评分中 / 完成 / 失败 — 不展示原始错误信息）
- 任务超时 5 分钟，超时返回已抓到的部分结果；详细失败原因写后端日志，不发前端

### F5：两阶段 AI 分析

**第一轮（DeepSeek V4-Flash 关闭 thinking）：类型筛选 + 标注**

- 判断每个岗位的类型是否与搜索关键词相关
- 顺便分类岗位类型（算法/产品/数据/工程/销售/其他）
- 不比对简历

**第二轮（DeepSeek V4-Flash 开启 thinking）：简历匹配**

- 输入：简历全文 + 用户求职目标方向 + soft preferences + 第一轮岗位类型 + 岗位 JD
- 按岗位类型路由不同 prompt（不同维度权重）
- 评分 prompt 含 4-6 个 few-shot 真实案例锚定（创始人自评的好/坏岗位）
- AI 内部走 chain-of-thought 推理（thinking 模式），前端**不展示原始 reasoning**，只展示结构化输出
- 输出（同时构成"可审计证据"，让用户能判断评分依据）：

```json
{
  "score": 8,
  "match_highlights": ["衍生品定价背景高度匹配", "CFA 持有者优先"],
  "gaps": ["缺乏 3 年以上风险模型开发经验"],
  "summary": "背景强相关，一个关键缺口可在简历中补充说明"
}
```

### F6：结果展示

- 按分数降序排列
- 每个岗位卡片：职位名、公司、薪资、评分（醒目显示）、匹配亮点（绿色）、差距（橙色）、一句话总结
- 实时进度推送（WebSocket）：扫码 → 爬取中 → 类型筛选中 → 匹配评分中

**评分标准：**

| 分数 | 含义                       |
| ---- | -------------------------- |
| 9-10 | 完全符合，强烈推荐         |
| 7-8  | 大部分符合，值得重点投递   |
| 5-6  | 部分符合，可以投递但需补强 |
| 3-4  | 匹配度低，建议低优先级     |
| 1-2  | 基本不符合                 |

---

## 非功能需求

### 隔离与并发

- 每个 `user_id` 对应服务端生成的 UUID profile 目录
- 单 profile 用 per-profile 互斥锁，同时只允许 1 个 Chrome 进程持有
- 全局并发上限 2-3 个 Chrome 进程；超过进队列
- 简历文本服务端按 `user_id` 隔离存储，TTL 24 小时

### 成本约束

- VPS：$24/月（4GB Vultr Tokyo）
- API：DeepSeek 月度预算上限 ¥100
- 限流（本期写死，不暴露给用户配置）：
  - 单用户每月最多 10 次搜索
  - 单次搜索最多 50 个岗位
  - 全局日预算 ¥4（≈ 100/30），到 80% 预警，到 100% 拒绝新任务
- 不上订阅制；后期若需防滥用走 1 元 / 5 毛 per use

### 数据保留

- 用户简历文本：服务端按 `user_id` TTL 24 小时；提供"删除简历"按钮立即清
- 浏览器 profile（含 Boss cookie）：按 `last_access` 计 30 天不活跃自动清理；提供"删除 Boss 登录态"按钮立即清
- 搜索结果：按 `task_id` TTL 保存 24 小时（支持刷新页面恢复展示）

### 反爬

- patchright（反检测 Playwright 分支）
- 持久化 profile 保留登录 token（`__zp_stoken__`），减少触发反爬频率
- 落点 query 校验，避免落到错关键词页

### 日志记录

每次任务（搜索 + 评分）都要落结构化日志，便于事后分析与算法迭代。

**记录粒度**

- 任务级：`task_id` / `user_id`(脱敏) / 开始时间 / 结束时间 / 总耗时 / 关键词 / 城市 / 数量
- 阶段级：爬取耗时、爬到岗位数、第一轮筛选保留数、第二轮评分耗时、API token 使用量
- 错误级：堆栈、Boss 反爬触发情况、AI 解析失败的 raw response（前 200 字）
- 评分级：每个岗位的输入 prompt 摘要、输出 score、AI 评分耗时

**存储**

- 走 systemd journald + 单独写一份 JSON Lines 到 `/var/www/boss_automation/logs/tasks/<YYYY-MM-DD>.jsonl`
- 日志按天分文件，保留 90 天后自动归档/删除
- 敏感字段（简历全文、Boss cookie）**不进日志**，只记 hash 或长度

**用途**

- 算法迭代：拉日志看哪类岗位评分一致性差，针对性改 prompt
- 成本审计：累加 token 用量校准月度预算
- 反爬诊断：被反爬时回看上下文（页面状态、URL、settle 轨迹）

---

## 超出范围（本期不做）

- Google / 微信等第三方账号系统
- 订阅制 / 完整账单系统
- 市场分析（行业趋势、技能热度）
- 自动投递功能
- 多 AI 提供商切换（单 DeepSeek 即可）

---

## 验证标准

工具是否成功的核心指标（决定要不要扩散到第 2、第 3 个用户）：

1. **算法可信度**：创始人自评 10 个熟悉岗位与 AI 评分吻合度 ≥ 70%
2. **真实使用**：1 个朋友实测 1 次完整搜索后，主动问 ≥ 2 个具体问题（非客套）
3. **资源可控**：2 个并发用户时 VPS 内存 < 80%、API 月成本 < ¥100

任一指标未达标，停止扩散到下一个用户，继续迭代算法或架构。
