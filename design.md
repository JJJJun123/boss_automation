# Boss 直聘智能岗位匹配 — 需求文档

## 背景

原版 Boss 直聘自动化项目已搁置 6 个月，功能过于复杂（含市场分析、市场认知分析等旁路）。现在要砍掉所有无用功能，聚焦核心。

## 问题与价值

**问题：** 求职者在 Boss 直聘搜索关键词后，平台仅展示岗位列表。用户需要逐一打开岗位、阅读 JD，才能判断是否值得投递。

**价值：** 通过 AI 自动阅读用户简历 + 每个岗位的 JD，给出 1-10 分匹配评分和具体说明，帮助用户在大量搜索结果中快速决定投递优先级。

## 差异化价值

- Boss 直聘平台没有任何智能匹配机制，仅展示岗位列表
- 本工具对每个岗位给出 1-10 分评分 + 具体匹配亮点和差距说明
- 跨关键词搜索结果统一排序，帮助决定投递优先级

## 目标用户

正在主动求职、需要在大量岗位中高效决策的用户。行为模式：高分岗位重点投递，低分岗位也会投递（平衡型）。

---

## 核心功能需求

### F1：简历上传

- 支持 PDF / DOCX 格式
- 解析简历全文（直接传文本给 AI，不做额外结构化）
- 简历仅 session 内临时存储，不持久化

### F2：岗位搜索

- 用户输入：关键词、城市、数量（50-100 个）
- 系统通过 Playwright 自动爬取 Boss 直聘

### F3：两阶段 AI 分析

**第一轮（快速廉价模型 如GLM）：类型过滤**

- 判断每个岗位的类型是否与搜索关键词相关
- 不比对简历，只做类型判断
- 过滤明显不相关的岗位（如搜"数据分析"混入"销售"岗位）

**第二轮（主力模型 如 Claude/ChatGPT）：简历匹配**

- 输入：简历全文 + 每个通过筛选的岗位 JD
- 输出：

```json
{
  "score": 8,
  "match_highlights": ["衍生品定价背景高度匹配", "CFA持有者优先"],
  "gaps": ["缺乏3年以上风险模型开发经验"],
  "summary": "背景强相关，一个关键缺口可在简历中补充说明"
}
```

### F4：结果展示

- 按分数降序排列
- 每个岗位卡片：职位名、公司、薪资、评分（醒目显示）、匹配亮点（绿色）、差距（橙色）、一句话总结
- 实时进度推送（WebSocket）：爬取中 → 类型筛选中 → 匹配评分中

**评分标准：**

| 分数 | 含义                       |
| ---- | -------------------------- |
| 9-10 | 完全符合，强烈推荐         |
| 7-8  | 大部分符合，值得重点投递   |
| 5-6  | 部分符合，可以投递但需补强 |
| 3-4  | 匹配度低，建议低优先级     |
| 1-2  | 基本不符合                 |

---

## 非功能需求

- 简历不持久化，session 内临时存储
- 每次搜索独立，无历史记录
- 多 AI 提供商支持，通过 `config/secrets.env` 配置

---

## 超出范围

- 市场分析（行业趋势、技能热度）

---

## 技术选型理由

**两阶段选型理由：** GLM 做类型过滤（速度快、成本低），主力模型做深度匹配。50-100 个岗位全量送给昂贵模型成本不现实；第一轮过滤后通常只剩 30-60 个，显著降低成本。

**实现方式：** 复用现有 Flask + Playwright + AIClientFactory 骨架，删除市场分析旁路，重构 EnhancedJobAnalyzer 为两阶段流水线。
