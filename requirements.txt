# Core web scraping
# patchright：Playwright 反检测分支，从驱动层避免 Runtime.enable 等 CDP 痕迹，
# 用于绕过 Boss security.html?code=37 反爬死循环。需配套 `patchright install chrome`。
patchright>=1.60.0
# playwright 仍需安装：其它模块用 playwright.async_api 的 Page/ElementHandle 做类型提示。
# 注意：须 >=1.60，旧版自带 chromium 在 macOS 26 上启动即崩溃(SIGSEGV)
playwright>=1.60.0
aiohttp>=3.9.0              # 异步 HTTP(部分 AI 客户端使用)
requests==2.31.0
beautifulsoup4==4.12.2

# Configuration and utilities
python-dotenv==1.0.0
PyYAML==6.0.1

# Web framework
Flask==3.0.0
Flask-CORS==4.0.0
Flask-SocketIO==5.3.6
python-socketio==5.10.0

# Document processing (for resume parsing)
python-docx==1.1.0
PyPDF2==3.0.1

# AI SDK - 官方客户端库
openai>=1.0.0          # OpenAI GPT模型
anthropic>=0.7.0       # Claude模型
google-generativeai>=0.3.0  # Gemini模型

# 注意：DeepSeek使用OpenAI兼容接口，无需额外SDK
# 注意：GLM（智谱）需要安装 zhipuai 库（如果使用）

# Testing
pytest>=8.0.0